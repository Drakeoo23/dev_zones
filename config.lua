Config = {
    Zones = {
        ['gym'] = {coords = vector3(-1195.6, -1577.7, 4.2), zone = 'chill', radius = 45.0 } --//add the zone u want it following this structure
        --['name zone'] = {coords = vector3(coords), zone = 'only chill', radius = your radius }
    }
}